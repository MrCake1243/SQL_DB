from models import *
# with db:
#     products = Product.select()
#     for p in products:
#         print(p.id, p.name, p.price, p.category_id.name)
#         p.price = p.price * 0.9
#         p.save()